# Task-2
This Python program is designed to illustrate different set operations using two predefined sets, E and N. Sets are a collection of unique elements, and Python provides built-in methods to perform various operations on them. In this program, the following set operations are performed: Union: Combines all elements from both sets without duplication
